{
  "name": "aidevs-r2d2",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch src/exercises/003/app.ts",
    "build": "tsc",
    "start": "bun run dist/exercises/003/app.js",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:restart": "docker-compose restart"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.4.1",
    "openai": "^4.26.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "typescript": "^5.3.3"
  }
} 